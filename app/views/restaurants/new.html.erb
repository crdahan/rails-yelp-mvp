<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, collection: ["pick a restaurant", 'chinese', 'italian', 'japanese', 'french', 'belgian'], default: %[] %>
  <%= f.submit %>
<% end %>
